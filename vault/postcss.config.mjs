// Tailwind v4 ships its own PostCSS plugin; there is no tailwind.config.js.
// All theme configuration lives in styles/globals.css under @theme.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
